---
import { getCollection } from "astro:content";
import SiteLayout from "../layouts/SiteLayout.astro";
import { sortDocs } from "../lib/docs";
import { currentAlphaVersion } from "../lib/package";

const docs = sortDocs(await getCollection("docs"));
---

<SiteLayout
  title="Bankstack"
  description="Bankstack docs for the alpha create-bankstack CLI and split-stack conventions."
>
  <div class="page">
    <section class="hero" aria-labelledby="page-title">
      <div>
        <p class="eyebrow">Alpha docs foundation</p>
        <h1 id="page-title">Bankstack</h1>
        <p class="lede">
          Scaffolding and conventions for pnpm-only Nx monorepos built around a
          Cloudflare edge plus Supabase split stack.
        </p>
        <div class="actions" aria-label="Primary actions">
          <a class="button" data-kind="primary" href="/docs/distribution/"
            >Choose a path</a
          >
          <a class="button" href="/docs/generated-workspace/"
            >View generated shape</a
          >
        </div>
      </div>
      <div class="stack-map" aria-label="Bankstack generated workspace map">
        <div class="stack-map__row">
          <span class="stack-map__label">Apps</span>
          <span class="stack-map__value">Astro, SvelteKit, Hono Workers</span>
        </div>
        <div class="stack-map__row">
          <span class="stack-map__label">Packages</span>
          <span class="stack-map__value">UI, Supabase, shared utilities</span>
        </div>
        <div class="stack-map__row">
          <span class="stack-map__label">Runtime</span>
          <span class="stack-map__value">Node 22.12+, pnpm 10+, Nx</span>
        </div>
      </div>
    </section>

    <section class="section" aria-labelledby="docs-title">
      <div>
        <p class="eyebrow">Read the docs</p>
        <h2 id="docs-title">Start with distribution</h2>
      </div>
      <div class="section__body">
        <p>
          Start with the adoption path, then use the generated workspace and
          boundary docs to connect real services intentionally.
        </p>
        <div class="doc-grid">
          {
            docs.map((doc) => (
              <a class="doc-link" href={`/docs/${doc.id}/`}>
                <span>{doc.data.title}</span>
                <small>{doc.data.description}</small>
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <section class="section" id="install" aria-labelledby="install-title">
      <div>
        <p class="eyebrow">Install</p>
        <h2 id="install-title">Run the CLI or use the skill</h2>
      </div>
      <div class="section__body">
        <p>
          The current repo alpha is <code>{currentAlphaVersion}</code>. Use the
          CLI for greenfield workspaces, or the Bankstack expert skill for
          existing projects and agent sessions.
        </p>
        <div class="command-list" aria-label="Alpha install commands">
          <pre><code>pnpm dlx create-bankstack@alpha my-bankstack-app</code></pre>
          <pre><code>npx create-bankstack@alpha my-bankstack-app</code></pre>
        </div>
        <p>
          <a href="/docs/distribution/">Read the distribution guide</a> for
          limits, skill usage, and feedback paths.
        </p>
      </div>
    </section>

    <section class="section" id="shape" aria-labelledby="shape-title">
      <div>
        <p class="eyebrow">Generated shape</p>
        <h2 id="shape-title">Local-first by design</h2>
      </div>
      <div class="section__body">
        <p>
          The alpha scaffold generates a workspace you can inspect, install,
          build, and adapt locally. It does not provision Cloudflare resources,
          create Supabase projects, configure production secrets, or deploy the
          app for you.
        </p>
        <p class="note">
          This docs app is intentionally static for the foundation slice. Future
          content now lives in Astro content collections without adding server
          rendering.
        </p>
      </div>
    </section>
  </div>
</SiteLayout>
