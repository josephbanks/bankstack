# {{PROJECT_NAME}}

This is a Bankstack workspace generated by `create-bankstack`.

## Requirements

- Node.js {{NODE_ENGINE}}
- pnpm {{PNPM_ENGINE}}

## Commands

```bash
pnpm install
pnpm dev
pnpm check
pnpm build
pnpm test
```

The root workspace is ready for the Bankstack app and package templates that will be added by later generator slices. This foundation does not create an app, require external service credentials, or include a Python compute layer.
