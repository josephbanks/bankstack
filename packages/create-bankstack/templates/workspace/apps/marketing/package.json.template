{
  "name": "@{{PROJECT_NAME}}/marketing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4321",
    "build": "astro check && astro build",
    "check": "astro check",
    "preview": "astro preview --host 127.0.0.1 --port 4321"
  },
  "dependencies": {
    "@{{PROJECT_NAME}}/shared-utils": "workspace:*",
    "@{{PROJECT_NAME}}/ui": "workspace:*",
    "astro": "{{DEPENDENCY_ASTRO}}"
  },
  "devDependencies": {
    "@astrojs/check": "{{DEV_DEPENDENCY_ASTROJS_CHECK}}",
    "typescript": "{{DEV_DEPENDENCY_TYPESCRIPT}}"
  }
}
