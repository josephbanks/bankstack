# Supabase Boundary

This directory is a placeholder for Supabase project files once you connect a real project. The generated scaffold does not provision Supabase, create product tables, or install a full auth flow.

Use `packages/supabase` for shared TypeScript env contracts and client helpers. Add migrations here only after you have a real schema design, then enable row level security on every exposed table before using browser or API clients.
