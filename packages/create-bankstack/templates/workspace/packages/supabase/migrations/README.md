# Supabase Migrations

Place generated Supabase migration files here after you connect a real Supabase project. The v0.1 scaffold intentionally does not create product tables.

Before exposing a table through Supabase APIs, enable row level security and add explicit policies for `anon` and `authenticated` roles.
