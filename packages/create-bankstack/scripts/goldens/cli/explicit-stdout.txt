create-bankstack
version 0.1.0-alpha.2

Resolved options:
  Project name: golden-stack
  Target directory: golden-stack
  Install dependencies: no
  Initialize git: no
  Force non-empty directory: no
  AI tooling guidance: none

Rendered workspace foundation with 49 files.

Next steps:
  cd golden-stack
  Dependency installation was skipped; run pnpm install when ready.
  Git initialization was skipped; run git init if desired.
